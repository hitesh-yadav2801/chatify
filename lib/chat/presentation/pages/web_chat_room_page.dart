import 'package:flutter/material.dart';

class WebChatRoomPage extends StatelessWidget {
  const WebChatRoomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.all(10));
  }
}
